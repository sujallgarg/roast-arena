/**
 * Centralized Application & Performance Constants
 */

// Live Battle Polling Configuration (in milliseconds)
export const BATTLE_POLL_INTERVAL_LIVE_MS = 10000; // 10s for active live battles
export const BATTLE_POLL_INTERVAL_HIDDEN_MS = 60000; // 60s when tab is in background (document.hidden)

// Comments Pagination & Limits
export const COMMENTS_PAGE_SIZE = 25;
